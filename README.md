This project contains SQL queries performed on the database for data analysis using SELECT, JOIN, GROUP BY, subqueries, and views.  
All queries were executed in DB Browser for SQLite and include screenshots of the outputs.
